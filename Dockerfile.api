FROM python:3.12-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY netguardian ./netguardian
COPY .env.example .
RUN mkdir -p data
EXPOSE 8000
CMD ["uvicorn", "netguardian.api:app", "--host", "0.0.0.0", "--port", "8000"]

